name: demo pipeline

on:
  push:
    branches: patch
  
jobs:
  build:
    name: test
    runs-on: ubuntu latest
    steps:
     - uses: actions/checkout@v4

     - name: run one line script
       run: echo Hello world!
